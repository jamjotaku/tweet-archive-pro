version: "3.9"

services:
  app:
    build: .
    container_name: tweet-archive-pro
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - TZ=Asia/Tokyo
      - SECRET_KEY=change-this-to-a-secure-random-string-in-production
